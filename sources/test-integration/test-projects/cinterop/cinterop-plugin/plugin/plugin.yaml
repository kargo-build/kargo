tasks:
  provideDef:
    action: !provideDef
      settings: ${pluginSettings}
      defFile: ${taskOutputDir}/custom.def

generated:
  cinteropDefinitions:
    - defFile: ${tasks.provideDef.action.defFile}
      fragment:
        modifier: ${pluginSettings.targetFragment}
