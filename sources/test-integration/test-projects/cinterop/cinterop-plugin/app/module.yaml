product: macos/app

plugins:
  plugin:
    enabled: true
    packageName: com.example.native
    code: |
      #include<stdio.h>
      static inline void hello() {
        printf("Hello from native code!\n");
      }
