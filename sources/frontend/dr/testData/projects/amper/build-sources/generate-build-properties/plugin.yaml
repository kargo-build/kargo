tasks:
  generate:
    action: !generateBuildProperties
      taskOutputDirectory: ${taskOutputDir}
      version: ${module.settings.publishing.version}

generated:
  sources:
    - language: kotlin
      directory: ${tasks.generate.action.taskOutputDirectory}
