product:
  type: ios/app
  platforms: [ iosArm64 ] # no simulator platforms, only devices -> requires the use of --device-id

dependencies:
  - $compose.foundation
  - $compose.material3

settings:
  compose: enabled
