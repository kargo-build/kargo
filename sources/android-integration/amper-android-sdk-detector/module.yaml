product: jvm/lib

description: Android SDK discovery utilities.

apply:
  - ../../common.module-template.yaml
  - ../../used-in-gradle.module-template.yaml
  - ../../used-in-idea.module-template.yaml

dependencies:
  - ../../core
  - ../../libraries/system-info

settings:
  jvm:
    # Used in Android Gradle builds (max 17) and IDEA (max 21), so we take the lowest version 17
    release: 17
  publishing:
    enabled: true
