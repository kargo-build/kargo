
tasks:
  some-task:
    action: !com.example.someAction
      boolean1: ${pluginSettings.boolean}
      boolean2: ${pluginSettings.requiredString} # ${pluginSettings.boolean}+true
      requiredString: "prefix-${pluginSettings.requiredString}-${boolean1}"
      nullablePath: ${pluginSettings.path}/${requiredString}
      path: ${pluginSettings}
      nullableInt: ${pluginSettings.nullablePath}
      nested1: ${module}
      nested2: ${pluginSettings.nested.map}
      dependencies:
        - hel:lo/foo
        - one-part
        - foo:bar:12:compile
        - ${markOutputsAs}
        - []
      requiredString2: ${pluginSettings.requiredString.missing}-${pluginSettings.unknown}-${unknownRoot.a}"
      unknownProperty1: hello
      unknownProperty2: -128
      unknownProperty3: ${unknownProperty2}
      unknownProperty4: ${dependencies}
      unknownProperty5: ${module.name}
      unknownProperty6: ${module.settings}
      unknownProperty7: ${module.settings.kotlin.languageVersion}
      path2: ${taskOutputDir}/abc
      unknownProperty8: ${path2} # path
      unknownProperty9: ${path2}/hello # path
      unknownProperty10: ${path2}/hello/${requiredString} # path
      unknownProperty11: hello/${requiredString}/nested # string
    taskOutputDir: ./hack/ed

module: {}